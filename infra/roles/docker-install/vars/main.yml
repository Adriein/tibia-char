docker_apt_repository: >
  deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc]
  https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable
remote_admin_user: aclaretdev
docker_group: docker
